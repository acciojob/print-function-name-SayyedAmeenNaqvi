//your JS code here. If required.
function functionName() {
    // This alerts the name of the current function
    alert(arguments.callee.name);
}

// You can call it like this to test:
functionName();
AccioJob();   